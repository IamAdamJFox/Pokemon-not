// const axios = require('axios');

// async function getMoveDetails(moveName) {
//     try {
//       const apiUrl = `https://pokeapi.co/api/v2/move/${moveName}/`;
//       const response = await axios.get(apiUrl);
//       const moveData = response.data;
//       const { accuracy, priority, power } = moveData;
//       return { accuracy, priority, power };
//     } catch (error) {
//       console.error('Error fetching move details:', error.message);
//       throw error;
//     }
//   }

